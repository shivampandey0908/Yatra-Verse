import type { Guide } from "../lib/api";
export function GuideCard({ guide }: { guide: Guide }) { const name = guide.userId?.name ?? "Verified local guide"; return <article className="rounded-[1.6rem] border border-violet-100 bg-white p-6 shadow-sm transition duration-300 hover:-translate-y-1 hover:shadow-float"><div className="flex items-start justify-between gap-4"><div className="flex items-center gap-3.5"><div className="grid h-12 w-12 place-items-center rounded-full bg-[linear-gradient(135deg,#d8cbfa,#f3d1bd)] font-serif text-lg font-bold text-plum">{name.slice(0, 1)}</div><div><h2 className="font-serif text-xl text-ink">{name}</h2><p className="mt-0.5 text-xs font-semibold tracking-wide text-plum">VERIFIED LOCAL GUIDE</p></div></div><span className="rounded-full bg-lavender px-2.5 py-1 text-xs text-plum">★ {guide.rating.average.toFixed(1)}</span></div><p className="mt-5 min-h-[66px] line-clamp-3 text-sm leading-6 text-slate-600">{guide.bio || "Profile details are being completed by this guide."}</p><div className="mt-5 flex min-h-6 flex-wrap gap-1.5">{guide.languages.slice(0, 3).map((language) => <span key={language} className="rounded-full border border-violet-100 px-2.5 py-1 text-xs font-medium text-slate-600">{language}</span>)}</div><div className="mt-6 flex items-center justify-between border-t border-slate-100 pt-4 text-sm"><span className="font-semibold text-ink">{guide.pricePerDay ? `₹${guide.pricePerDay.toLocaleString()} / day` : "Price on request"}</span><span className="text-slate-400">{guide.rating.count} reviews</span></div></article>; }
