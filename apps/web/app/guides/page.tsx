import { guides } from "../../lib/api"; import { GuideCard } from "../../components/guide-card";
export const revalidate = 60;
export default async function GuidesPage() { let items = [] as Awaited<ReturnType<typeof guides>>["data"]; let unavailable = false; try { items = (await guides()).data; } catch { unavailable = true; } return <section className="mx-auto max-w-7xl px-5 py-16"><p className="text-sm font-bold tracking-[.18em] text-plum">LOCAL GUIDE MARKETPLACE</p><h1 className="mt-3 font-serif text-5xl text-ink">Meet India through its people.</h1><p className="mt-5 max-w-2xl leading-7 text-slate-600">YATRA VERSE only displays guides whose profiles are approved for public discovery.</p>{unavailable ? <div className="mt-12 rounded-2xl border border-amber-200 bg-amber-50 p-6 text-amber-900">Guide discovery is temporarily unavailable. Please try again shortly.</div> : items.length ? <div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map((guide) => <GuideCard key={guide._id} guide={guide} />)}</div> : <div className="mt-12 rounded-2xl border border-violet-100 bg-white p-8 text-slate-600">No approved guides are listed yet. Check back as guide verification opens.</div>}</section>; }
