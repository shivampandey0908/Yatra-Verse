import { destinations } from "../../lib/api"; import { DestinationCard } from "../../components/destination-card";
export const revalidate = 60;
export default async function DestinationsPage() { let items = [] as Awaited<ReturnType<typeof destinations>>["data"]; let unavailable = false; try { items = (await destinations()).data; } catch { unavailable = true; }
 return <section className="mx-auto max-w-7xl px-5 py-16"><p className="text-sm font-bold tracking-[.18em] text-plum">DESTINATION DISCOVERY</p><h1 className="mt-3 font-serif text-5xl text-ink">Find your next perspective.</h1><p className="mt-5 max-w-2xl leading-7 text-slate-600">Explore destinations curated by YATRA VERSE. Each page brings local discovery and transparent intelligence into one considered view.</p>{unavailable ? <div className="mt-12 rounded-2xl border border-amber-200 bg-amber-50 p-6 text-amber-900">Destination discovery is temporarily unavailable. Please try again shortly.</div> : items.length ? <div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{items.map((item) => <DestinationCard key={item._id} destination={item} />)}</div> : <div className="mt-12 rounded-2xl border border-violet-100 bg-white p-8 text-slate-600">Destinations will appear here once they are published.</div>}</section>; }
